#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int N;

    while (cin >> N) {
        if (N) cout << "vai ter duas!\n";
        else cout << "vai ter copa!\n";
    }

    return 0;
}